torch>=2.1
transformers>=4.40
datasets>=2.18
peft>=0.10
accelerate>=0.29
numpy
pyyaml
tqdm

# Only needed by the embedding-based selectors (alg/miwv.py)
sentence-transformers>=2.6

# Only needed for the fast eval backend (--eval-backend vllm); CUDA GPU required
# vllm>=0.6
